<div class="container">
    <h1>Our chefs are:</h1>
    <div class="row">
            {{#each cooks}}
                <div class="col-4">
                    <img src="{{image}}" alt="{{name}} img">
                    <h5>{{name}}</h5>
                    <p><a href="/cooks/deleteCook?id={{_id}}" class="links">Delete Chef</a></p>
                    <p><a href="/cooks/updateCook?id={{_id}}" class="links">Update Chef</a></p>
                </div>
            {{/each}}
    </div>
</div>  